<UserControl x:Class="XTMF.Gui.UserControls.ModuleContextControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XTMF.Gui.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid  Background="{DynamicResource WindowBackgroundBrush2}">



        <Label Content="Some Text"></Label>

        <ListView MouseUp="UIElement_OnMouseUp"  BorderThickness="0"  Background="{DynamicResource WindowBackgroundBrush2}"   x:Name="ModulePathList"  ContextMenuOpening="FrameworkElement_OnContextMenuOpening">

            <ListView.ItemContainerStyle>

                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderBrush" Value="Transparent"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush2}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ContextMenu  >
                <ContextMenu Name="ModuleContextMenu" Placement="Bottom">

                </ContextMenu>
            </ListView.ContextMenu>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal">

                    </StackPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Tag="{Binding Path=.}"  Orientation="Horizontal">




                        <Border BorderThickness="0,1,0,1"  BorderBrush="{DynamicResource WindowBackgroundBrush4}">
                            <Border.Style>
                                <Style TargetType="{x:Type Border}">
                                    <Style.Triggers>

                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=StackPanel}, Path=IsMouseOver}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource FocusBrushHover2}">

                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=StackPanel}, Path=IsMouseOver}" Value="False">
                                            <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush2}">

                                            </Setter>
                                        </DataTrigger>

                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource FocusBrushHover3}">

                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>


                                </Style>
                            </Border.Style>
                            <StackPanel   Orientation="Horizontal"  HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Label Foreground="{DynamicResource ThemeTextColorBrush}" Tag="{Binding Path=.}"  Content="{Binding Path=Name}" MouseDoubleClick="Control_OnMouseDoubleClick" HorizontalAlignment="Center">
                                    <Label.Style >
                                        <Style TargetType="{x:Type Label}">
                                            
                                            
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="White">

                                                    </Setter>

                                                    
                                                </DataTrigger>
                                            </Style.Triggers>
                                            
                                        </Style>
                                    </Label.Style>
                                </Label>




                            </StackPanel>
                        </Border>
                        <Grid>
                            <Polyline StrokeThickness="2" Points="0,0 14,14, 0,28" Stroke="{DynamicResource WindowBackgroundBrush4}" Fill="{DynamicResource WindowBackgroundBrush2}" />
                            <Polyline StrokeThickness="2" Points="0,0 13,14, 0,28">
                                <Polyline.Style>
                                    <Style TargetType="{x:Type Polyline}">
                                        <Style.Triggers>

                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=StackPanel}, Path=IsMouseOver}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource FocusBrushHover2}">

                                                </Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=StackPanel}, Path=IsMouseOver}" Value="False">
                                                <Setter Property="Fill" Value="{DynamicResource WindowBackgroundBrush2}">

                                                </Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                                <Setter Property="Fill" Value="{DynamicResource FocusBrushHover3}">

                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Polyline.Style>
                            </Polyline>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</UserControl>

