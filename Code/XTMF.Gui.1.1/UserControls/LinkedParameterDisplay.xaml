<!-- 
    Copyright 2014-2015 Travel Modelling Group, Department of Civil Engineering, University of Toronto

    This file is part of XTMF.

    XTMF is free software: you can redistribute it and/or modify
    it under the terms of the GNU General Public License as published by
    the Free Software Foundation, either version 3 of the License, or
    (at your option) any later version.

    XTMF is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU General Public License for more details.

    You should have received a copy of the GNU General Public License
    along with XTMF.  If not, see <http://www.gnu.org/licenses/>.
-->
<Window x:Class="XTMF.Gui.UserControls.LinkedParameterDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:my="clr-namespace:XTMF.Gui"
                WindowStyle="ToolWindow"
                WindowStartupLocation="CenterOwner"
                Title="Linked Parameter Editor" Height="480" Width="640">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="AUTO" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <my:BorderIconButton Grid.Column="0" Header="New" Text="Create new Linked Parameter" Clicked="NewLinkedParameter_Clicked"
                                 Icon="/XTMF.Gui;component/Resources/Plus.png" Margin="2"/>
            <my:BorderIconButton Grid.Column="1" Header="Remove" HighlightColour="{StaticResource WarningRed}" Text="Remove Current Parameter" Clicked="RemoveLinkedParameter_Clicked"
                                 Icon="/XTMF.Gui;component/Images/Delete.png" Margin="2"/>
            <my:BorderIconButton Grid.Column="2" Header="Select" HighlightColour="{StaticResource WarningRed}" Text="Create new Linked Parameter" Clicked="RemoveLinkedParameter_Clicked"
                                 Icon="/XTMF.Gui;component/Images/Delete.png" Margin="2"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="330" />
                <ColumnDefinition Width="AUTO" />
                <ColumnDefinition Width="*" MinWidth="200"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="AUTO" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <my:FilterBox Grid.Row="0" x:Name="LinkedParameterFilterBox" />
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ListView x:Name="Display"  Background="{StaticResource WindowBackgroundBrush}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.Resources>
                            <Style TargetType="{x:Type ListViewItem}">
                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            </Style>
                        </ListView.Resources>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <my:BorderIconButton Margin="4" Header="{Binding Name}" ShadowColour="{StaticResource ControlHeaderColour}"
                                             Icon="/XTMF.Gui;component/Resources/Settings.png" Width="300px">
                                    <my:BorderIconButton.Style>
                                        <Style TargetType="my:BorderIconButton">
                                            <Setter Property="Selected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        </Style>
                                    </my:BorderIconButton.Style>
                                </my:BorderIconButton>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>
            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" IsTabStop="False" Background="White" ShowsPreview="True" />
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="AUTO" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <my:FilterBox Grid.Row="0" x:Name="ContainedParameterFilterBox" />
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ListView x:Name="ContainedParameterDisplay"  Background="{StaticResource WindowBackgroundBrush}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.Resources>
                            <Style TargetType="{x:Type ListViewItem}">
                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                            </Style>
                        </ListView.Resources>
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <my:BorderIconButton Margin="4" Header="{Binding Name, Mode=OneTime}" ShadowColour="{StaticResource ControlHeaderColour}"
                                                     Text="{Binding ModuleName, Mode=OneTime}"
                                             Icon="/XTMF.Gui;component/Resources/Settings.png" Width="300px">
                                    <my:BorderIconButton.Style>
                                        <Style TargetType="my:BorderIconButton">
                                            <Setter Property="Selected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        </Style>
                                    </my:BorderIconButton.Style>
                                </my:BorderIconButton>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
